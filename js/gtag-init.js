// Google Analytics (GA4) initialisation.
// Kept in a local file (served from 'self') so it passes the theme's
// Content-Security-Policy without needing 'unsafe-inline'.
window.dataLayer = window.dataLayer || [];
function gtag() { dataLayer.push(arguments); }
gtag('js', new Date());
gtag('config', 'G-YKSMEZ50TG');
